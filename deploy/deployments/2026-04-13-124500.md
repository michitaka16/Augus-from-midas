# Deploy: 2026-04-13 12:45 SGT

## Environment
- **Target**: Development (docker-compose + local services)
- **Commit**: d718773acabc2bd0c088e91e804c01ec4bae6728

## Gates
- [x] Docker, uv, node, npm available
- [x] Postgres + Redis healthy (docker-compose)
- [x] Migrations applied (0001_initial_schema + 0002_debate_history)
- [x] Seed data loaded (12 ETFs, ~1008 bars, 1 signal)

## Services
- [x] API server: port 8000, 39 routes, governance assertions PASSED
- [x] Web frontend: port 3000, Next.js 14, all 9 pages rendering

## Verification
- [x] Health check: `GET /health` → `{"status": "ok"}`
- [x] Smoke test: `GET /signals/latest` → 1 signal, regime=normal, allocations present
- [x] User-visible: `curl localhost:3000` contains "Midas"

## Notes
- TimescaleDB hypertable creation commented out in migration (standard Postgres, no extension)
- Python 3.14 editable install .pth files not processed — workaround: scripts add package src dirs to sys.path
- aiohttp `web.run_app()` incompatible with `asyncio.run()` in Python 3.14 — fixed with `AppRunner` + `TCPSite`
