name: Backtest Regression

on:
  pull_request:
    paths:
      - "packages/midas-strategy/**"
      - "packages/midas-backtest/**"
      - "packages/midas-data/src/midas_data/models/**"

jobs:
  backtest-regression:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
      - run: uv venv && uv sync
      - run: echo "Backtest regression gate — wired in M03-09"
