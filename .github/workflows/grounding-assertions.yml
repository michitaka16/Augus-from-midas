name: Grounding Assertions

on:
  pull_request:
    paths:
      - "packages/midas-debate/**"

jobs:
  grounding-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
      - run: uv venv && uv sync
      - run: echo "Grounding assertion gate — wired in M10-10"
